# Database Configuration
# Valores por defecto que coinciden con la pipeline
POSTGRES_USER=admin
POSTGRES_PASSWORD=td8corrupcion
POSTGRES_DB=corrupcion_db
POSTGRES_PORT=5433

# API Configuration
API_PORT=8000

# Database URL
# Opción 1: Si usas el docker-compose del backend (servicio 'db')
DATABASE_URL=postgresql://admin:td8corrupcion@db:5432/corrupcion_db

# Opción 2: Si te conectas a la DB de la pipeline (servicio 'postgres')
# DATABASE_URL=postgresql://admin:td8corrupcion@postgres:5432/corrupcion_db

# Opción 3: Para desarrollo local (PostgreSQL en localhost)
# DATABASE_URL=postgresql://admin:td8corrupcion@localhost:5433/corrupcion_db
